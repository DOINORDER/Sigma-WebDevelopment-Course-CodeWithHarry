let a = 6

function factorial(number){
    let arr = Array.from(Array(number =).keys())
    console.log(arr)
    let c = arr.slice(1, ).reduce(a, b) => {
        return a*b
    }

    console.log(c)
}

factorial(a)